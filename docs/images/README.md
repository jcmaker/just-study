# README 이미지

손으로 그린 SVG 네 장은 저장소에 들어 있습니다. 실제 화면을 찍어야 하는 세 장은 비어 있고,
README에서는 주석으로 감싸 두었습니다. 파일을 넣은 뒤 해당 주석만 풀면 됩니다.

| 파일 | 쓰이는 곳 | 무엇을 담나 |
|---|---|---|
| `banner.svg` | README 맨 위 | 워드마크와 30일 그리드 |
| `architecture.svg` | "무엇을 하는 물건인가" 아래 | 에이전트·서버·저장소 분리, 서버에 LLM이 없다는 사실 |
| `day-flow.svg` | "하루가 어떻게 흘러가나" 아래 | 퀴즈와 보완 학습을 오가는 순환 |
| `rubric.svg` | "자료를 믿을 수 있는 이유" 아래 | 100점 배점 비율과 80점 선정 기준선 |
| `dashboard-today.png` | 아직 없음 | 진행 중인 과정의 `오늘` 탭 |
| `themes.png` | 아직 없음 | 같은 화면을 테마별로 찍어 나란히 붙인 것 |
| `codex-flow.gif` | 아직 없음 | Codex에서 하루를 진행하는 장면 |

## SVG 네 장에 관해

전부 Focus 테마 규칙을 따릅니다. 흰 종이, 검정 잉크, 빨강 `#c60000`, 노랑 `#ffd84a`,
라운드 0, 굵은 스트로크. 폰트는 Helvetica Neue와 SF Mono 계열만 씁니다. 원격 폰트를
받지 않으므로 다른 환경에서는 시스템 대체 폰트로 그려집니다.

빨강은 하나의 뜻으로만 씁니다. **그 화면에서 가장 중요한 제약.** `day-flow.svg`에서는
회고를 막는 채점 관문, `rubric.svg`에서는 80점 기준선, `architecture.svg`에서는 서버가
LLM을 호출하지 않는다는 사실입니다. 새 그림을 그릴 때도 빨강을 장식으로 쓰지 마세요.

너비는 1280으로 맞춰 두었습니다. GitHub에서 `width="100%"`로 들어가므로 세로가 너무
길어지지 않게 460~660 사이를 유지하는 편이 좋습니다.

다크 모드용 대체본은 두지 않았습니다. Focus 테마 자체가 흰 종이라 흑백 반전이 제품의
모습과 어긋납니다. 배너와 같은 선택입니다.

## 찍기 전 준비

빈 데이터로 찍으면 화면이 허전합니다. 과정이 몇 개 있는 상태에서 찍으세요.
직접 만든 학습 데이터가 있으면 그대로 쓰면 되고, 없으면 서버를 띄운 뒤 Codex에서
`$just-study`로 과정 하나를 만들어 Day 1까지 진행해 두면 충분합니다.

```bash
npm run dev
```

## dashboard-today.png

1. 진행 중인 과정의 `오늘` 탭을 엽니다 (`/courses/<id>?tab=today`)
2. 브라우저 창을 1280px 폭으로 맞춥니다
3. 목표 카드와 단계 표시(`강의 → 퀴즈 → 보완 학습 → 회고 → Day 완료`)가 함께 보이도록 스크롤합니다
4. macOS에서는 `⌘⇧4` 뒤 스페이스바로 창 전체를 찍습니다

퀴즈 단계에서 찍으면 사지선다 폼까지 함께 담겨 제품을 설명하기 좋습니다.

## themes.png

같은 화면을 다섯 테마로 찍어 이어 붙입니다. `/settings`에서 테마를 바꿔 가며
같은 위치·같은 스크롤로 찍어야 비교가 됩니다.

다섯 장을 다 넣으면 세로로 길어지니, Focus·Bubblegum·Terminal 세 장만 골라
가로로 붙이는 편이 읽기 좋습니다.

## codex-flow.gif

이 저장소에는 GIF를 만드는 도구가 없습니다. 화면 녹화 후 변환이 필요합니다.

macOS 기본 도구만 쓴다면 `⌘⇧5`로 화면 일부를 녹화해 `.mov`를 얻은 뒤,
ffmpeg가 있는 환경에서 변환하세요.

```bash
ffmpeg -i recording.mov -vf "fps=12,scale=1000:-1:flags=lanczos" -loop 0 codex-flow.gif
```

담을 장면은 이 정도면 충분합니다.

1. Codex에 `$just-study 계속` 입력
2. 에이전트가 저장된 Day와 단계를 읽어 오는 부분
3. 문제를 제시하고 답을 받는 부분
4. 브라우저로 전환해 같은 진도가 반영된 화면

10~20초, 5MB 이하를 권합니다. GitHub에서 큰 GIF는 로딩이 눈에 띄게 느립니다.
