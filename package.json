{
  "name": "just-study",
  "version": "0.2.0",
  "description": "Pick a topic and an agent researches sources, scores them, and builds a 30-day course you study one day at a time. Self-hosted, localhost only.",
  "license": "MIT",
  "author": "jcmaker <jcmaker0627@gmail.com>",
  "homepage": "https://github.com/jcmaker/just-study#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jcmaker/just-study.git"
  },
  "bugs": {
    "url": "https://github.com/jcmaker/just-study/issues"
  },
  "keywords": [
    "self-hosted",
    "learning",
    "study",
    "mcp",
    "model-context-protocol",
    "codex",
    "ai-agent",
    "nextjs",
    "sqlite",
    "markdown"
  ],
  "//": "private blocks npm publish. This is a self-hosted app, not a package, so it stays. It does not affect the MIT license or the public source.",
  "private": true,
  "type": "module",
  "//overrides": "next 16.2.12 is current and stays. It pins postcss 8.4.31 exactly and takes sharp ^0.34.5 as an optional dep, both of which carry advisories. These raise only those two transitive packages. Never downgrade next itself: npm audit suggests 9.3.3, which is years old.",
  "overrides": {
    "postcss": "^8.5.25",
    "sharp": "^0.35.3"
  },
  "engines": {
    "node": ">=22.23.1"
  },
  "scripts": {
    "dev": "next dev --hostname 127.0.0.1 --port 5878",
    "build": "next build",
    "start": "next start --hostname 127.0.0.1 --port 5878",
    "lint": "eslint .",
    "test": "node --test tests/platform-foundation.test.ts tests/learning-engine.test.ts tests/mcp.test.ts tests/codex-skill.test.ts tests/dashboard.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "next": "^16.0.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@tailwindcss/postcss": "4.3.3",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "eslint": "^9.0.0",
    "eslint-config-next": "^16.0.0",
    "tailwindcss": "4.3.3",
    "typescript": "^5.9.0"
  }
}
